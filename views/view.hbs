<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Sentiment Analysis</title>
  <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
  <div class="container mt-5">
    <h1>{{categoryName}}</h1>

    <ul class="nav nav-tabs" id="myTab" role="tablist">
      <li class="nav-item" role="presentation">
        <a class="nav-link active" id="positives-tab" data-toggle="tab" href="#positives" role="tab" aria-controls="positives" aria-selected="true">Positives</a>
      </li>
      <li class="nav-item" role="presentation">
        <a class="nav-link" id="negatives-tab" data-toggle="tab" href="#negatives" role="tab" aria-controls="negatives" aria-selected="false">Negatives</a>
      </li>
    </ul>

    <div class="tab-content mt-3" id="myTabContent">
      <div class="tab-pane fade show active" id="positives" role="tabpanel" aria-labelledby="positives-tab">
        <table class="table">
          <thead>
            <tr>
              <th scope="col">Result</th>
              <th scope="col">Date</th>
            </tr>
          </thead>
          <tbody>
            {{#each positives}}
              <tr>
                <td>{{text}}</td>
                <td>{{date}}</td>
              </tr>
            {{/each}}
          </tbody>
        </table>
      </div>
      <div class="tab-pane fade" id="negatives" role="tabpanel" aria-labelledby="negatives-tab">
        <table class="table">
          <thead>
            <tr>
              <th scope="col">Result</th>
              <th scope="col">Date</th>
            </tr>
          </thead>
          <tbody>
            {{#each negatives}}
              <tr>
                <td>{{text}}</td>
                <td>{{date}}</td>
              </tr>
            {{/each}}
          </tbody>
        </table>
      </div>
    </div>

    <ul class="pagination mt-3">
      {{#each pages}}
        <li class="page-item {{#if active}}active{{/if}}">
          <a class="page-link" href="#">{{number}}</a>
        </li>
      {{/each}}
    </ul>
  </div>

  <script src="https://code.jquery.com/jquery-3.3.1.slim.min.js"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.7/umd/popper.min.js"></script>
  <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.min.js"></script>
</body>
</html>
